# 31. 디자인 2단계 브리프 — AI 랜딩페이지 문법 제거 + 고유한 브랜드 레이아웃

> 2026-09-22 창업자가 준 디자인 2단계 지시를 **원문 그대로** 남긴다. 1단계(타이포그래피)는 `26-typography-system.md` 로 이미 적용됐고, 이번에는 글꼴을 다시 크게 바꾸지 않는다.
> 이전 기준(`21`, `24`, `25`)과 부딪히면 **이 문서가 이긴다.** 진행 기록(무엇을 찾았고 무엇을 바꿨는지, Before → After)은 맨 아래 §진행 기록에 단계별로 적는다.
> 한 줄 원칙: "AI 가 만든 예쁜 사이트"가 아니라 "내가짠데이라는 브랜드가 원래 이렇게 생긴 것처럼 보이는 사이트". 기능 · 데이터 · API · 사용자 흐름은 하나도 지우지 않는다(화면 계약서 `npm run verify` 가 지킨다).

---

## 원문

현재 1단계 Typography 작업은 이미 적용되었다.
이제 폰트는 다시 크게 변경하지 말고,
사이트 전체의 레이아웃, 정보 구조, 컴포넌트 배치, 여백, 시각적 리듬을 재설계하라.

목표는 단순히 "더 예쁜 웹사이트"가 아니다.
현재 사이트에서 느껴지는 "AI가 자동으로 만든 프리미엄 스타트업 웹사이트" 느낌을 제거하고,
"내가짠데이라는 하나의 독립적인 생활 서비스 브랜드" 처럼 보이게 만들어라.

### 1. 절대 유지해야 하는 핵심
서비스의 핵심은 절대 바꾸지 않는다.
내가짠데이 = "예산을 말하면 AI가 하루 코스를 짜서 영수증으로 보여주는 서비스"

브랜드의 핵심 시각 언어: 하루 · 예산 · 돈 · 영수증 · 이동 · 코스 · 짠이.
이 7개를 사이트 전체의 디자인 언어로 사용하라.
새로운 기능을 임의로 추가하지 말 것. 기존 기능, 데이터, API, 사용자 플로우를 삭제하지 말 것.
이번 작업은 철저하게 "브랜드 경험과 레이아웃 개선"에 집중한다.

### 2. 가장 먼저 해야 할 작업
전체 페이지를 하나씩 보면서 다음과 같은 전형적인 AI 웹사이트 패턴을 모두 찾아라.
- 대형 중앙 정렬 Hero
- 거대한 한 줄 슬로건
- 반복되는 둥근 카드
- 모든 정보를 카드 안에 넣는 구조
- 카드 → 카드 → 카드 식의 수직 나열
- 동일한 border-radius 반복
- 지나치게 동일한 여백
- 모든 요소가 가운데 정렬
- CTA 버튼이 모든 섹션에서 반복
- "AI가 ~해드립니다" 식의 과도하게 친절한 문장
- 너무 완벽하게 대칭적인 레이아웃
- SaaS dashboard 같은 섹션 구성
- 의미 없는 장식용 gradient
- 의미 없이 floating 하는 UI
- 모든 정보를 box 안에 가두는 방식

이러한 패턴이 발견되면 단순히 스타일을 바꾸지 말고 레이아웃 구조 자체를 재검토하라.

### 3. 내가짠데이만의 레이아웃 언어 구축
사이트 전체에서 아래 4개의 레이아웃 패턴을 반복적으로 사용하라.

**A. RECEIPT LAYOUT** — 영수증을 단순한 카드로 사용하지 말고 서비스의 핵심 인터페이스 언어로 사용한다.

```
오늘의 예산
₩50,000
────────────────
식사             ₩18,000
카페              ₩8,000
데이트            ₩20,000
────────────────
남은 금액          ₩4,000
```

이 구조를 Landing / Result / Share / My / Summary 등에 자연스럽게 반복한다.
단, 모든 것을 실제 영수증 모양으로 만들지는 말 것. 영수증의 구분선 · 항목 구조 · 숫자의 위계 · 상하 흐름 · tear-off feeling 만 가져온다.

**B. ROUTE LAYOUT** — 이동 경로를 단순한 지도 기능이 아니라 브랜드 그래픽 언어로 사용한다.
(집 ↓ 점심 ↓ 카페 ↓ 전시 ↓ 저녁) 지도에만 route를 두지 말고 페이지 내부의 콘텐츠 흐름에도 route의 개념을 적용하라.

**C. DAY FLOW** — 사용자가 하루를 "시간순으로 소비한다"는 점을 시각적으로 보여준다.
(12:00 점심 · 14:00 카페 · 16:30 산책 · 18:30 저녁) 각 장소를 무조건 카드로 만들지 말고 timeline / list / receipt item / image block 등을 섞어서 사용한다.

**D. MONEY AS VISUAL OBJECT** — 가격은 단순한 작은 텍스트가 아니다.
총 예산 · 사용 금액 · 남은 금액 · 장소별 예상 비용은 크기와 공간을 충분히 사용한다.
다만 가격을 모든 화면의 가장 큰 숫자로 만들지는 말고 "필요한 순간 강하게 보이게" 한다.

### 4. Landing Page 재구성
Hero를 다시 점검한다. 절대로 "대형 문구 + 오른쪽 카드" 형태의 전형적인 AI SaaS Hero를 만들지 않는다.
Hero는 "서비스 사용 장면"처럼 보여야 한다. 사용자가 페이지에 들어왔을 때 예산 → 오늘의 하루 → 영수증 이라는 흐름이 자연스럽게 보이도록 구성한다.
예시 방향: "오늘 쓸 수 있는 돈이 이만큼이라면," [₩50,000] 오늘은 점심 → 카페 → 산책 → 저녁 그리고 마지막에 [오늘의 영수증].
문구를 억지로 위 예시와 동일하게 만들 필요는 없다. 중요한 것은 "광고를 보는 느낌"보다 "서비스가 어떻게 작동하는지 직접 보는 느낌"이다.
Hero에서 큰 제목 하나 · CTA 하나 · 장식 몇 개 만 보여주는 방식에서 벗어나라. 서비스 자체가 Hero가 되게 한다.

### 5. Section 간 연결 방식 개선
현재 페이지가 Hero ↓ 카드 ↓ 카드 ↓ 카드 ↓ CTA 처럼 끊겨 보이지 않는지 확인한다.
각 섹션이 이전 섹션의 결과처럼 느껴져야 한다.
(예산 입력 ↓ 오늘의 조건 ↓ AI가 짠 하루 ↓ 영수증 ↓ 지도/이동 ↓ 실제로 하루를 보내는 모습)
섹션을 독립적인 마케팅 블록으로 만들지 말고 하나의 사용자 여정처럼 연결한다.

### 6. 카드 사용 제한
카드는 기본 레이아웃이 아니라 "정보를 묶어야 할 때만 사용하는 컴포넌트"로 바꾼다. 모든 섹션이 카드로 보이면 안 된다.
카드 대신: 큰 여백 · divider · timeline · receipt line · full-width image · text block · editorial layout · horizontal flow · map · 리스트.
장소 정보는 모든 장소를 동일한 카드로 반복하지 않는다.
핵심 장소 → 큰 이미지 + 핵심 정보 / 보조 장소 → compact row / 시간 정보 → timeline / 가격 → receipt item.

### 7. 비대칭 레이아웃 적극 활용
모든 것을 50:50으로 나누지 않는다(60/40 · 70/30 · 40/60).
실험적인 포트폴리오 사이트처럼 난해하게 만들지는 말 것. 목표는 "규칙은 있지만 템플릿처럼 보이지 않는 디자인".

### 8. 한국적인 느낌의 구현 방식
한옥/기와/한지 그래픽으로 직접 표현하지 않는다. 대신 한국어 문장의 줄바꿈 · 여백 · 종이 질감 · 영수증 · 골목과 도시의 선형 구조 · 간판/지도에서 느껴지는 도시성 · 소박한 정보 디자인 · 생활 서비스의 친숙함.
"한국적인 AI 서비스"가 아니라 "한국에서 실제로 만들어져 사람들이 쓰는 서비스" 처럼 보여야 한다.

### 9. 짠이 캐릭터 사용 방식
decorative sticker처럼 반복해서 붙이지 않는다. 등장 빈도를 줄이고 역할을 부여한다.
예산을 알려줄 때 · 코스가 완성될 때 · 예산이 부족할 때 · 장소를 바꿨을 때 · 사용자를 안내할 때 — "상황에 반응하는 companion". 무조건 화면에 떠 있는 캐릭터가 아니다.

### 10. 버튼 / CTA 재설계
CTA를 모든 섹션마다 크게 넣는 구조를 제거한다. 실제로 행동해야 하는 지점에서만 강조한다.
예산 입력 → "이 예산으로 짜기" / 코스 결과 → "이 장소 바꾸기" / 완성 → "영수증 저장하기".
"시작하기 / 자세히 보기 / AI 추천 받기" 같은 추상적인 CTA 남발을 줄인다.

### 11. Result Page는 가장 중요하게 재설계
마케팅 웹사이트 느낌을 완전히 제거한다. "그래서 오늘 어디 가면 되는데?"를 3초 안에 이해할 수 있어야 한다.
첫 화면에 바로: 오늘의 예산 · 남은 금액 · 오늘의 코스 · 예상 이동시간 · 전체 소요시간.
그 다음 하루의 흐름(12:00 점심 · 14:00 카페 · 16:30 이동 · 18:00 전시 · 19:30 저녁).
지도와 리스트가 경쟁하지 않게: 지도는 "어디로 이동하는지", 리스트는 "무엇을 하는지".

### 12. 모바일은 별도로 생각
Desktop을 줄여서 Mobile로 만들지 않는다. 예산 ↓ 오늘의 코스 ↓ 시간 ↓ 장소 ↓ 이동 ↓ 영수증 순.
Result 화면은 Bottom Sheet / Sticky Summary / Timeline 등으로 지도와 일정이 서로 가려지지 않도록 한다.

### 13. 시각적 리듬
모든 섹션의 padding을 동일하게 적용하지 않는다. 큰 섹션 → 큰 여백 / 정보 밀도가 높은 영역 → 작은 여백 / Hero → 넓게 / 영수증 → 촘촘하게 / 장소 리스트 → 읽기 좋게.
"8px grid를 지키는 것"보다 "콘텐츠의 의미에 따라 밀도가 달라지는 것"을 우선한다.

### 14. 이미지 사용
이미지는 카드 안에 넣는 장식물이 아니다. 핵심 장소는 이미지를 크게, 보조 장소는 이미지가 없어도 성립하도록.
모든 장소에 동일한 비율의 이미지가 들어가서 Pinterest처럼 반복되지 않게 한다.
이미지가 없는 경우 억지 placeholder 이미지를 채우지 않는다. 데이터 신뢰도가 낮다면 텍스트 정보가 중심이 되도록 한다.

### 15. 최종 디자인 테스트
실제로 실행해서 최소 Landing · Plan Wizard · Course Result · Place Detail · Save/Share · My · Mobile Landing · Mobile Result 를 직접 확인한다. 각 화면에서:
- 이 화면이 일반적인 AI SaaS처럼 보이는가?
- 카드가 너무 많은가?
- 중앙정렬이 과한가?
- 서비스의 핵심인 돈 / 하루 / 영수증 / 이동이 보이는가?
- 사용자가 무엇을 해야 하는지 바로 이해되는가?
- 짠이가 이유 없이 등장하지 않는가?
- **이 화면만 캡처해도 "내가짠데이"라고 알아볼 수 있는가?** (가장 중요)

"예쁜가?"보다 "다른 서비스로 대체할 수 없는가?"를 기준으로 수정한다.

### 16. 금지사항
Apple clone · Notion clone · Linear clone · generic AI SaaS · glassmorphism · neon gradient · 지나친 3D · 과도한 blur · 둥근 카드 남발 · dashboard 느낌 · 전형적인 AI sparkle icon · "AI" 텍스트 장식 남발 · 전통문화 이미지를 이용한 억지 한국풍 · 의미 없는 애니메이션 추가.

### 17. 작업 순서
먼저 전체 페이지를 분석한다. 그 다음:
1. 현재 레이아웃 문제점 정리
2. 가장 심각한 AI-template 패턴 선정
3. Global layout 수정
4. Landing 수정
5. Plan 수정
6. Result 수정
7. Place card 수정
8. Mobile 수정
9. 전체 spacing / component consistency 수정
10. 실제 브라우저에서 visual QA

기존 기능과 API는 유지한다. 마지막에는 "Before → After에서 무엇이 달라졌는지"를 디자인 관점에서 간단히 정리한다.

---

## 진행 기록

### 1. 지금 화면에서 찾은 AI 템플릿 문법 (2026-09-22, 캡처 56장 · 데스크톱 1440 / 모바일 Pixel 7)

| 화면 | 찾은 것 | 브리프 항목 |
|---|---|---|
| 랜딩 히어로 | **"큰 세리프 슬로건 왼쪽 + 떠 있는 영수증 카드 오른쪽"** — 금지된 바로 그 SaaS 히어로. 뒤에 옅은 한옥 사진 · 바닥에 옅은 지붕선 그림(장식). 짠이가 영수증 모서리에 스티커처럼 붙어 있다 | 대형 Hero · 장식 · 짠이 스티커 |
| 랜딩 전체 | 장면마다 같은 머리 "01 ─ ─ ─ 과거 × 현재"가 다섯 번. 섹션 사이는 바탕색 띠(종이 ↔ 눌린 종이)로만 끊긴다 → 히어로 ↓ 블록 ↓ 블록 ↓ CTA | 섹션이 끊김 · 같은 여백 |
| 01 과거 × 현재 | 둥근 사진 위에 둥근 사진을 겹쳐 띄움(떠 있는 UI) | floating UI |
| 02 세 단계 | 단계마다 짠이 얼굴 하나씩(3번) — 장식 | 짠이 남발 |
| 03 무엇이 다른가 | 아이콘 + 태그 + 제목 + 본문의 2×2 기능 격자 = SaaS "Features" | SaaS 구성 · 아이콘 |
| 04 어떤 약속 | 둥근 사진 다섯 장의 핀터레스트 격자 | 같은 비율 사진 반복 |
| 05 나의 하루 | 사진 위 짙은 네이비 배너 + 흰 CTA 두 개 + 큰 짠이. 헤더의 파란 "무료로 추천받기"와 합쳐 **같은 CTA 가 한 화면에 셋** | CTA 반복 |
| 헤더 | 모든 화면에 파란 알약 CTA | CTA 반복 |
| 코스 결과 | 오른쪽 칸 첫 화면 = 날짜 칩 → 짠이 말풍선 → 친구 안내 상자 → 알약 탭 → **세로 600px 영수증 카드**. "오늘 어디 가지?"의 답(장소 순서)이 1.5화면 아래에서야 시작한다. 합계가 세 곳(말풍선 · 영수증 · 세 칸 통계)에서 반복. "총 소요 / 도보 이동 / 이동 거리" 세 칸은 대시보드 | 3초 안에 이해 · 대시보드 |
| 코스 결과 · 장소 | 네 곳이 모두 같은 모양의 흰 둥근 카드(16:9 사진 + 칩 + 본문 + 도구 줄). 중요도 구분 없음 | 카드 반복 |
| 모바일 결과 | 지도 40% 아래 시트의 첫 화면이 말풍선 · 안내 · 탭 · 영수증 머리. 장소는 세 번째 화면부터 | 모바일 순서 |
| 위저드 | 질문마다 흰 둥근 카드, 지역은 둥근 카드 격자(아이콘 + 이름 + 숫자 + ›) | 모든 것을 상자에 |
| 내 코스(빈 상태) | 가운데 큰 짠이 + 가운데 제목 + 버튼 둘 | 중앙 정렬 |

**가장 심각한 셋**: ① 히어로의 "슬로건 + 카드" 구도 ② 결과 화면이 답(하루의 순서)보다 장식(말풍선 · 영수증 카드)을 먼저 보여 주는 순서 ③ 모든 것이 같은 반지름의 흰 상자라는 것.

### 2. 레이아웃 언어 — 네 가지 부품으로만 짠다

1. **영수증 줄(ledger)** — `이름 ········ 금액`. 구분선은 점선(`receipt-rule`), 끝은 톱니 · 구멍 줄(`tear-line`). 실제 영수증 모양(`<Receipt>`)은 **한 화면에 한 번**(랜딩 03 · 결과 화면의 "오늘의 영수증" · 위저드 옆)만.
2. **경로(route)** — 점(들르는 곳) + 선(이동). 가로로는 히어로 · 결과 화면 요약의 "하루 한 줄", 세로로는 결과 화면의 일정 · 랜딩의 단계.
3. **하루(day flow)** — 왼쪽에 시각(같은 폭 숫자), 가운데 선, 오른쪽에 할 일. 결과 화면의 일정이 이 모양이다.
4. **돈(money)** — 남은 돈 · 오늘 쓸 돈만 크게(`text-price-lg`). 장소 가격은 영수증 줄의 오른쪽 끝.

상자(흰 카드)는 정보를 묶어야 할 때만: 결과 화면에서는 "핵심 장소" 사진 한 장, 시트 · 대화상자. 반지름은 두 가지만: 사진 · 입력 8px, 칩 둥글게. 카드용 24px 반지름은 쓰지 않는다.

### 3. 적용한 것 (커밋 순서, 2026-09-22)

| 커밋 | 무엇 |
|---|---|
| 58afd45 | 전역: 카드 반지름 24 → 12px, 주 버튼 광택 제거, `tear-line` · `rail` 유틸리티, 헤더 CTA 제거(메뉴 밑줄). 랜딩: 히어로 = 서식 세 칸(오늘 쓸 돈 → 오늘의 하루 → 영수증), 장면 머리 = 경로 위 정거장, 기능 격자 → 장부 줄, 사진 격자 → 메뉴판, 네이비 배너 → 영수증의 뜯는 자리 |
| 89d2fa9 | 결과: `DaySummary`(남은 돈 · 예산 · 쓰는 돈 · 오늘의 경로 · 소요/이동) → 하루의 흐름(시각 칸 · 점선 · 순번이 선 위의 점 = 지도 핀 번호) → "오늘의 영수증". 핵심 장소 한 곳만 큰 사진, 나머지는 줄. 밑줄 탭. AI sparkle 아이콘 제거 |
| 32382fd | 위저드: 질문 = 점선으로 나눈 서식 칸, 지역 = 노선도식 목록. 빈 상태: 짠이 옆에 문장(가운데 정렬 제거). 내 코스: 장부 줄 |
| f116094 | 랜딩 첫 방문 시퀀스(docs/32 A): 덮개 화면 없이 히어로 자신이 경로 → 예산 count-up → 영수증 출력 → 짠이 → 행동 순서로(약 2.8초, 세션당 한 번, CSS). 모션 최소화에서 지연도 0. 예전 지붕선 인트로 삭제 |
| 70f5726 | 취향 단계 = 짧은 질문 셋 + "더 자세히" + "좋아요. 이렇게 이해했어요."(4단계 유지, docs/30 초안을 한 단계 안으로). 결과 조건 = 아이콘 칩 |
| 3050b1c | (API) 여행 v2 지연: 계획 하나 안의 같은 후보 조회를 한 번만 — 1박 v2 11.7s → 5.3s, 결과 동일(docs/29 §5) |
| 9334042 | 로딩 = 경로가 이어지고 영수증 줄이 찍힘(스피너 · 떠다니는 캐릭터 · 동전 제거). 장소 줄: 태그는 글 한 줄, 예시 사진은 사진 칸에 "예시" 표시 |
| 73392cb | QA 1차 수정(아래 §5) |

### 4. 부품 체계 (docs/32 B §24) — 페이지마다 새로 만들지 않는다

| 부품 | 어디에 | 규칙 |
|---|---|---|
| Button | `ui/button.tsx` | 행동하는 자리만 `brand`(단색 파랑). 높이 `md` 44 · `xl` 56, 반지름 lg/xl(10~12px). 광택 · 떠오름 · 파란 그림자 없음. 보조는 `soft` 또는 밑줄 글자 링크(최소 44px 높이) |
| Chip | 결과 조건 · 위저드 선택 | 조건 = `bg-paper-2 rounded-md h-8` + 아이콘. 고르는 칩 = `rounded-full border-[1.5px] min-h-11`, 고르면 파랑 |
| 고르는 칸(선택 카드) | 위저드 | `rounded-lg border-[1.5px]`, 그림자 없음, 고르면 `border-blue-deep bg-blue-soft` |
| 서식 칸 | 위저드 질문 · 히어로 | 위쪽 점선(`border-dashed border-ink/20`) 하나로 나눈다. 흰 상자에 넣지 않는다 |
| ReceiptRow / Receipt | `brand/Receipt.tsx` | 이름 ···· 금액. 실제 영수증 모양은 한 화면에 한 번 |
| tear-line | 히어로 · 결과 요약 · 랜딩 끝 | 뜯는 자리 = 큰 구획의 끝 |
| RouteItem | `brand/DayRoute.tsx` | 점 = 들르는 곳, 점선 = 이동. 넓으면 가로, 좁으면 세로. 그려질 때마다 차례로 놓인다 |
| TimelineItem | `course/CourseTimeline.tsx` | 시각 칸 3.25rem · 점선 · 순번(= 지도 핀 번호) · 할 일 |
| PlaceCard | `course/StopCard.tsx` | 핵심 한 곳만 큰 사진(16:10), 나머지는 64px 사진 줄. 1차 이름 · 시각 · 가격 / 2차 주소 · 태그 한 줄 · 이유 / 3차 왜 여기 · 거리뷰 · 지도 앱 · 바꾸기 |
| BudgetDisplay | `course/DaySummary.tsx` · 히어로 | 남은 돈만 `text-price-lg`(금색 = 돈), 예산 · 쓰는 돈은 작은 장부 |
| Jjani | 히어로(남은 돈) · 결과 요약 · 알림 · 빈 상태 · 로딩 · 랜딩 끝 | 반응할 때만. 떠다니는 스티커 금지 |
| Modal / BottomSheet | `ui/dialog` · `ui/sheet` · 결과 모바일 시트 | 카드 반지름(12px). 시트의 지도는 절반 단계 34dvh |
| Motion | `globals.css` 모션 언어 | 움직이는 것은 돈(셈) · 경로(그림) · 영수증(출력) · 짠이(숨) 넷. transform/opacity/clip-path 만 |

### 5. QA 2차 (docs/32 C) — `node e2e/qa-pass.mjs <폴더>` 로 누구나 다시 돌릴 수 있다

사용자 흐름 그대로(랜딩 → 위저드 4단계 → 로딩 → 결과 → 장소 상세 → 장소 바꾸기 → 공유 · 저장 → 내 코스 → 없는 코스 → 404)를 1440 · 1280 · 430 · 390 에서 밟으며 화면별로 가로 넘침 · 잘린 글자 · 44px/24px 미만 터치 영역 · 깨진 사진 · 에러를 센다(결과는 `report.md`). 모션은 `node e2e/motion-audit.mjs <폴더>`.

- 1회차(72화면): 넘침 0 · 깨진 사진 0 · 에러 0. 잡힌 것 — **P0** 모바일 장소 줄의 거리뷰 · 지도 앱 버튼이 16px 로 눌림(flex shrink) / **P1** 구간의 "네이버 지도 길찾기" 21px · 동네 명소 링크 20px(WCAG 24px 미달), 랜딩 보조 링크 20px, 헤더 메뉴 41px, 동네 칩 · 예산 칩 · 날짜 칩 36~40px / 모바일 장소 이름이 "큐코인노래…"로 잘림 / 모바일 취향 카드 넷이 한 화면을 다 씀 / 모바일 결과 첫 화면에 첫 일정이 걸침.
- 고침(73392cb): 모두 44px 로(구간 링크는 음수 여백으로 줄 간격 유지), 이름 두 줄, 취향 카드 두 칸, 날짜+시간 한 칩, 모바일 지도 절반 단계 40 → 34dvh.
- 2회차: 24px 미만 0. 44px 미만으로 남은 것은 탭 글자 폭(높이 45) · 지도 SDK 컨트롤(우리 것이 아님) · 순번 점(가상 요소로 44px 영역) 뿐.
- 화면 계약서(`npm run verify`): 141건이 전부 **의도한 변경** — 히어로 제목 줄바꿈, 헤더 "무료로 추천받기" 제거, "이 예산으로 코스 받기" → "이 예산으로 짜기", 취향 단계의 태그 31개가 "더 자세히" 안으로(지운 것이 아니라 접은 것). **결과 화면은 사라진 것 0** — 에러 · 넘침 · 깨진 사진 0. `verify:record` 로 다시 기록.
- 모션 점검: 첫 방문 시퀀스 재생 · 모든 장면이 3.3초 뒤 온전 · 예산 50,000 끝까지 · 모션 최소화에서 처음부터 다 보임 · 같은 세션 두 번째 방문은 없음 · 지도 핀 순번대로 착지 — 전부 통과.

### 6. Before → After (디자인 관점)

- **히어로**: "큰 슬로건 + 오른쪽에 떠 있는 영수증 카드 + 옅은 한옥 사진" → 서식 한 장: 오늘 쓸 돈(손으로 만짐) → 오늘의 하루(경로) → 영수증(남은 돈 + 짠이의 한마디). 광고가 아니라 한 번 써 보는 장면.
- **랜딩 흐름**: 번호 붙은 광고 블록 다섯 개 → 한 줄의 점선이 장면들을 정거장처럼 잇는다. 기능 격자는 장부, 사진 격자는 메뉴판, 배너는 영수증의 끝.
- **결과 화면**: 말풍선 → 안내 상자 → 알약 탭 → 600px 영수증 카드 → (1.5화면 아래) 장소 → 첫 화면에 남은 돈 · 예산 · 오늘의 경로 · 소요/이동 → 시각 · 점선 · 할 일의 하루 → 마지막에 "오늘의 영수증". 같은 카드 넷 → 핵심 한 곳만 사진, 나머지는 줄.
- **위저드**: 흰 카드 더미 → 점선으로 나눈 서식. 취향 31개 태그 → 짧은 질문 셋 + 되읽어 주는 요약.
- **짠이**: 스티커(단계마다 · 영수증 모서리 · 배너) → 반응하는 순간에만(남은 돈 · 요약 · 알림 · 빈 상태 · 로딩 · 마지막 안내).
- **모션**: 덮개 인트로(지붕선 → 스카이라인) → 히어로 자신이 경로 · 돈 · 영수증 순서로 놓인다. 새 코스 · 새 예산마다 경로가 다시 그려진다.

### 7. 남은 것 · 리스크

- **가장 큰 UX 리스크**: 모바일 결과의 첫 화면. 지도(34dvh) · 조건 칩 · 탭 · 남은 돈이 위에 있어 390×844 에서 첫 일정은 요약 경로(점 목록)로만 보이고 첫 장소 줄은 한 번 스크롤해야 한다. 지도를 더 줄일지, 요약 경로를 가로 한 줄로 접을지는 창업자 판단.
- **가장 큰 visual 리스크**: 세리프 제목(Noto Serif)과 Jua 로고가 여전히 남아 "에디토리얼 AI 랜딩"의 인상을 일부 만든다 — 이번 지시는 "폰트는 크게 바꾸지 않는다"였다. 창업자 대기열의 타이포그래피 정리(폰트 최소화)가 다음이다.
- 개발 서버 한정: API 가 단일 프로세스라 장소 상세가 공연 조회(KOPIS 약 17초) 뒤에 줄 서 스켈레톤이 오래 보일 수 있다(엔드포인트 자체는 0.24초). 운영은 워커 여러 개.
- 장소를 바꾼 직후 줄이 제자리로 미끄러지는 약 0.3초 동안 시각 칸과 줄이 어긋나 보인다(layout 애니메이션). 정지 상태에서는 맞다.
- 결과 화면의 카카오맵 타일 색은 SDK 가 정한다(브랜드 톤으로 누를 수 없음). 핀 · 경로 색만 브랜드.
- `LottiePlayer` 는 로딩에서 빠져 이제 쓰는 곳이 없다(파일은 남겨 둠).
